@model HrmsCoreMvc.Models.PayRoll.EarningType


<button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
    Add Earning Type
</button>

<table class="table table-bordered table-hover" id="earningTypeTable">
    <thead class="table-light">
        <tr>
            <th>Id</th>
            <th>Earning Type Name</th>
            <th width="120">Action</th>
        </tr>
    </thead>

    <tbody>

        @foreach (var item in ViewBag.EarningTypeList)
        {
            <tr>
                <td>@item.EarntypeId</td>

                <td>@item.EarningName</td>

                <td>
                     <button type="button"
                        class="btn btn-primary btn-sm editBtn"
                        data-bs-toggle="modal"
                        data-bs-target="#editModal"
                        data-id="@item.EarntypeId"
                        data-name="@item.EarningName">
                        Edit
                    </button>

                    <a asp-action="DeleteEarningType"
                       asp-route-id="@item.EarntypeId"
                       class="btn btn-danger">
                       Delete
                        <i class="fa-solid fa-trash"></i>
                    </a>
                </td>
            </tr>
        }

    </tbody>
</table>


<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Add Earning Type</h5>
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form asp-action="AddEarningType" method="post">
            <div class="modal-body">
                <label>Earning Name</label>
                <input type="text" asp-for="EarningName" class="form-control" placeholder="Enter Earning Type Name">
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="submit" class="btn btn-primary">Add Earning Type</button>
            </div>
            </form>
        </div>
    </div>
</div>


<div class="modal fade" id="editModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title">Edit Earning Type</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>

            <form asp-action="EditEarningType" method="post">

                <div class="modal-body">

                   
                   <input asp-for="EarntypeId" type="hidden" id="editId" />

                    <div class="mb-3">
                        <label>Earning Name</label>
                        <input asp-for="EarningName" type="text" id="editName" class="form-control" />
                    </div>

                </div>

                <div class="modal-footer">
                    <button type="button"
                            class="btn btn-secondary"
                            data-bs-dismiss="modal">
                        Cancel
                    </button>

                    <button type="submit"
                            class="btn btn-success">
                        Update
                    </button>
                </div>

            </form>

        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {

            $('#earningTypeTable').DataTable({
                pageLength: 10,
                lengthMenu: [5,10,25,50],
                ordering: true,
                searching: true
            });

            $('.editBtn').click(function () {

    var id = $(this).data('id');
    var name = $(this).data('name');

    $('#editId').val(id);
    $('#editName').val(name);

});
        });
    </script>
}