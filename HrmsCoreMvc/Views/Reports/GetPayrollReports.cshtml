@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<HrmsCoreMvc.Models.Reports.PayslipViewModel>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

<style>
    .icon-box {
        width: 48px;
        height: 48px;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .bg-orange-subtle { background-color: #fff3eb; border: 1px solid #ffe1cc; }
    .text-orange { color: #fd7e14; }
    
    .bg-green-subtle { background-color: #e6f9f0; border: 1px solid #c3f2d9; }
    .text-green { color: #20c997; }
</style>

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h3 class="fw-bold mb-1">Payroll Report</h3>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0">
                <li class="breadcrumb-item"><a href="#">HR</a></li>
                <li class="breadcrumb-item active">Payroll Report</li>
            </ol>
        </nav>
    </div>

    <div class="dropdown">
        <button class="btn btn-outline-secondary btn-sm dropdown-toggle d-flex align-items-center gap-1" type="button" id="exportDropdown" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-box-arrow-up"></i> Export
        </button>
        <ul class="dropdown-menu dropdown-menu-end shadow-sm" aria-labelledby="exportDropdown">
            <li>
                <a class="dropdown-item" href="#" id="exportCsvBtn">
                    <i class="bi bi-file-earmark-excel text-success me-2"></i>Export to Excel
                </a>
            </li>
            <li>
                <a class="dropdown-item" href="#" id="exportPdfBtn">
                    <i class="bi bi-file-earmark-pdf text-danger me-2"></i>Export to PDF
                </a>
            </li>
        </ul>
    </div>
</div>

<!-- Main Row Containing Grid (Left 50%) and Chart (Right 50%) -->
<div class="row g-3 mb-4">

    <!-- LEFT HALF: 2x2 Metric Cards -->
    <div class="col-lg-6">
        <div class="row g-3 h-100">
            <div class="col-md-6 d-flex flex-column">
                <div class="card border-0 shadow-sm h-100 p-2 flex-fill">
                    <div class="card-body">
                        <div class="d-flex align-items-center gap-3">
                            <div class="icon-box bg-orange-subtle text-orange rounded-3 p-3">
                                <i class="bi bi-people-fill fs-4"></i>
                            </div>
                            <div>
                                <div class="text-muted small">Total Payroll</div>
                                <h3 class="fw-bold m-0"></h3>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-md-6  d-flex flex-column ">
                <div class="card border-0 shadow-sm h-100 p-2 flex-fill">
                    <div class="card-body">
                        <div class="d-flex align-items-center gap-3">
                            <div class="icon-box bg-green-subtle text-green rounded-3 p-3">
                                <i class="bi bi-diagram-3-fill fs-4"></i>
                            </div>
                            <div>
                                <div class="text-muted small">Deductions</div>
                                <h3 class="fw-bold m-0"></h3>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-md-6  d-flex flex-column ">
                <div class="card border-0 shadow-sm h-100 p-2 flex-fill">
                    <div class="card-body">
                        <div class="d-flex align-items-center gap-3">
                            <div class="icon-box bg-blue-subtle text-blue rounded-3 p-3">
                                <i class="bi bi-person-check-fill fs-4"></i>
                            </div>
                            <div>
                                <div class="text-muted small">Net Pay</div>
                                <h3 class="fw-bold m-0"></h3>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-md-6  d-flex flex-column ">
                <div class="card border-0 shadow-sm h-100 p-2 flex-fill">
                    <div class="card-body">
                        <div class="d-flex align-items-center gap-3">
                            <div class="icon-box bg-red-subtle text-red rounded-3 p-3">
                                <i class="bi bi-shield-lock-fill fs-4"></i>
                            </div>
                            <div>
                                <div class="text-muted small">Earnings</div>
                                <h3 class="fw-bold m-0"></h3>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- RIGHT HALF: Chart Container Card -->
    <div class="col-lg-6">
        <div class="card border-0 shadow-sm h-100 p-3">
            <div class="card-body d-flex flex-column">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <div class="d-flex align-items-center gap-2">
                        <i class="bi bi-bar-chart-line text-danger"></i>
                        <h6 class="fw-bold mb-0">Payroll</h6>
                    </div>
                    <select class="form-select form-select-sm w-auto">
                        <option>This Year</option>
                        <option>Last Year</option>
                    </select>
                </div>
                <div class="flex-grow-1 position-relative" style="min-height: 220px;">
                    <canvas id="payrollChart"></canvas>
                </div>
            </div>
        </div>
    </div>

</div>

<div class="card border-0 shadow-sm">
    <div class="card-body p-4">
        <form method="get" asp-controller="PayrollReport" asp-action="GetPayrollReports" id="filterForm">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h5 class="fw-bold m-0">Payroll List</h5>
                <div class="d-flex gap-2">
                    <select name="sortMonth" class="form-select form-select-sm w-auto" onchange="document.getElementById('filterForm').submit();">
                        <option value="">All Status</option>
                        <option value="Jan-March" selected="@(ViewBag.SelectedSortStatus == "Jan-March")">Jan-March</option>
                        <option value="April-Jun" selected="@(ViewBag.SelectedSortStatus == "April-Jun")">April-Jun</option>
                        <option value="July-Sept" selected="@(ViewBag.SelectedSortStatus == "July-Sept")">July-Sept</option>
                        <option value="Oct-Dec" selected="@(ViewBag.SelectedSortStatus == "Oct-Dec")">Oct-Dec</option>
                    </select>

                    <!-- Sorting Filter Dropdown -->
                    <select name="sortType" class="form-select form-select-sm w-auto" onchange="document.getElementById('filterForm').submit();">
                        <option value="">Sort By</option>
                        <option value="Ascending" selected="@(ViewBag.SelectedSortType == "Ascending")">Ascending</option>
                        <option value="Descending" selected="@(ViewBag.SelectedSortType == "Descending")">Descending</option>
                    </select>
               </div>
            </div>
            </form>
            <div class="table-responsive">
                <table id="PayrollTable" class="table align-middle table-hover w-100">
                    <thead class="table-light">
                    <tr>
                        <th>Payslip ID</th>
                        <th>Name</th>
                        <th>Paid Amount</th>
                        <th>Paid Month</th>
                        <th>Paid Year</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var pay in Model)
                    {
                        <tr>
                            <td>@pay.PayslipID</td>
                            <td>@pay.Name</td>
                            <td>@pay.Month</td>
                            <td>@pay.Year</td>
                        </tr>
                    }

                </tbody>
                </table>
            </div>
        </div>
</div>

 <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>




 @section Scripts {
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#ePayrollTable').DataTable({
                "dom": 'frtip'
            });
            $('.dropdown-toggle').dropdown();
        });

    </script>
}


<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.31/jspdf.plugin.autotable.min.js"></script>

 <script>
     document.getElementById('exportPdfBtn').onclick = (e) => {
         e.preventDefault();
         const doc = new jspdf.jsPDF('l');
         doc.autoTable({ 
            html: '#PayrollTable',
            didParseCell: (d) => { if (d.column.index === 1) d.cell.text = [d.cell.raw.innerText.trim()]; }
        });
        doc.save('Payroll_Report.pdf');
    };
</script>

<script>
    document.getElementById('exportCsvBtn').onclick = function(e) {
    e.preventDefault();
    const rows = document.querySelectorAll('#PayrollTable tr'); 
    const cvrows = Array.from(rows).map(row => {
        const cells = row.querySelectorAll('th,td');
        const cellTexts = Array.from(cells).map(cell => `"${cell.innerText.trim()}"`);
        return cellTexts.join(',');
    });

    const csvContent = cvrows.join('\n');
    const link = document.createElement('a');
    link.href = 'data:text/csv;charset=utf-8,' + encodeURIComponent(csvContent);
    link.download = 'Payroll_Report.csv';
    link.click();
    };

</script>
     